id: "db"
type: "library"
language: "typescript"

tasks:
  build:
    command: "pnpm run build"
  generate:
    command: "pnpm run generate"
    local: true
  migrate:
    command: "pnpm run migrate"
    local: true
