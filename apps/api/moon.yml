id: "api"
type: "application"
language: "typescript"

tasks:
  dev:
    command: "pnpm run dev"
    local: true
  build:
    command: "pnpm run build"
    deps:
      - "core:build"
      - "db:build"
  db-migrate-local:
    command: "pnpm run db-migrate-local"
    local: true
