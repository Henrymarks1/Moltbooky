id: "web"
type: "application"
language: "typescript"

tasks:
  dev:
    command: "pnpm run dev"
    local: true
  build:
    command: "pnpm run build"
    deps:
      - "core:build"
  test:
    command: "pnpm run test"
